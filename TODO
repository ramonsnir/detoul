 0 `detoul edit` (if there was a change) takes time. fix it.
 1 check that branch/ref exists
 2 rename all take's/rebase's
 3 deterministic commit hashes?
 * hints, warnings and are-you-sure's
 * archive old spec files
 * list detoul minimal dependencies (for packages, and general documentation)
 * Arch package
 * proper Windows installer
 * Unit tests
 * better options definition
 * detoul specs-push --force
 * detoul specs-pull --force
 * no checkout in take
 * config: per repo, per global, per spec
-5 RHEL package
-4 Debian package
-3 interactive spec creation - going well :)
-2 octopus-take support?
-1 Mac installation?
